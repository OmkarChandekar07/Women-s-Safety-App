import 'package:flutter/material.dart';

class ContacPage extends StatelessWidget {
  const ContacPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Contacts Page")),
    );
  }
}